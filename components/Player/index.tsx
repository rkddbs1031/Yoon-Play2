import { PlayerBar } from './PlayerBar';
import PlayerPanel from './PlayerPanel';

const Player = () => {
  return (
    <>
      <PlayerBar />
      <PlayerPanel />
    </>
  );
};

export default Player;
